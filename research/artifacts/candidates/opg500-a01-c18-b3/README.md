# C18 B3 continuation

Verdict: candidate_only. Best verified result: none. NONTERMINAL_CHECKPOINT.

The manifest and payloads preserve all NEW source/proof/checker/input/output
and runtime members byte-for-byte. Member paths are virtual until restored;
they are NOT separately expanded repository files. Restore with the existing
T01 unpack.py into a fresh directory; it never executes code.

New work: first-hit/consecutive-visit localization in the unchanged C16 Walk;
structural Mathlib Walk adapter and derived external-path intersection;
constructed simple children with at least three vertices, strict real lengths
and exact integer/mod-two identities; finite q-odd minimum descent source.
The full paper B3 proof and both root-tail comparisons are archived.

Actual Python ordinary and optimized executions returned zero and identical
outputs:18 fixtures,520 strict competitors,1040 endpoint/circle symmetry
replays,19 mutations. No5913-pattern recount. A long cheap external shortcut
shows both shorter children can have MORE edges, excluding edge-count descent.

All new Lean remains UNCOMPILED here. The intrinsic circle metric/actual-arcs
adapter is still a missing full Lean body; the source does not pretend this
premise was proved by naming a matrix D. External two-environment build reports
were not replayed. Only ShortcutAlgebra's exact supplied repair digest was
recovered; the claimed corrected FiniteReal bytes were not obtained. No root
stub was replaced, no independent verifier or admission gate passed.

No old C14/C15/C16/C17/T01-T03 objects are retransmitted. One new packet belongs
to the root obligation. See Issue3 for the final transport and open-lemma state.
