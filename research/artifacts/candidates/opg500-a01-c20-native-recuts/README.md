# C20 native recut agreement and direct B3 composition

Status: NONTERMINAL_CHECKPOINT. Verdict: candidate_only.
Best verified result: none. ALL new Lean source is UNCOMPILED.
Only repository: vibemathing/problem-opg-500-geodesic-cycles.
Problem: problem:opg-500-geodesic-cycles.
Attempt/route/graph: attempt:web-20260906-opg500-a01 /
route:geodesic-linear-encoding-v1 / graph:opg500-initial-v1.
Target: obligation:opg500-root.
Read base: df9fb18cf6d4f3e4836bbb6e86be77995abcdcf5.

## One continuation, distinct immutable inputs

This complete archive preserves all 21 files in the original C19-circle-metric
attachment (including its historical packet as a passive candidate member) and
15 new C20 members. Nothing from PR23-27 or T01-T03 is retransmitted.
Nine chunks and this manifest preserve actual bytes, not only source digests.
Logical member paths are NOT separately expanded GitHub paths. The unchanged
T01 unpack.py can restore them without executing code. No alias is required.

The pre-existing branch web/attempt-opg500-a01-c19-circle-arcs is reused. Its
OTHER C19-circle-arcs archive is a different implementation. At the recorded
read revision only its README, manifest and first two of five chunks existed.
See transport-status.json for the three missing expected chunk hashes. These
fragments are preserved unchanged, are NOT an executable or complete archive,
and are NOT dependencies of the complete archive here. The current packet
must not claim those 20 other logical members have been recovered or verified.
Their remaining original bytes must be recovered rather than regenerated from
claims or guessed from this differently named C19 implementation.

## Actual atomic mathematical advance

RecutAgreement.lean proves exact endpoint-indexed Mathlib Walk equality of the
prefix and rotation arc pairs, up to swapping both components together. For
ordered distinct endpoints, it derives C=a++b++c, rotate_x(C)=b++c++a and
rotate_y(C)=c++a++b from take_spec and native append cancellation. The simple
first factors are derived from IsCycle, not supplied. It then transports
IsPath, vertex/undirected-edge supports and costs by the SAME walk equality,
and derives support union, endpoint-only intersection and integer edge counts.

B3Native.lean begins with an actual CycleView (base, native closed Walk,
IsCycle), strictly positive undirected real weights and an arbitrary strict
competing Walk. It provides native simple erasure, derives the actual circle
metric laws through the frozen C19 modules, applies minimum violating factor
extraction, derives endpoint-only meeting, constructs two genuine IsCycle
children with at least three vertices, proves strict weighted descent, and
proves integer incidence cancellation and F2 XOR. The final signature does not
accept an abstract B3 provider, externally supplied child cycles, triangle
law, arc support partition or Q-disjointness as assumptions.

OutsideSpan.lean calls that actual B3 theorem in the minimum-outside-submodule
argument, proves finite CycleView enumeration through a nodup-word injection,
and specializes to the span of actual native triangles. An outside seed is
still an explicit root-tail premise. The paper proof is complete for this
conditional slice; neither forest/C4 nor the 5913 modes were recounted here.

## Boundaries, counterpressure and execution

For x=y at the base of a triangle, prefix arcs are (nil, reverse whole cycle)
whereas rotate arcs are (nil, whole cycle); exact Walk equality fails. The
recut agreement theorem correctly requires x!=y. The diagonal distance is
handled separately. For reversed endpoint order the arc components must swap.
Undirected edge support is invariant under reversal; directed darts are not.
These distinguish false strengthened statements, not the correctly scoped
positive-real root counterexample.

The only new executed mathematical test is a bounded typed free-word/symbolic
identity checker: seven identities and eight mutation rejections plus bad
endpoint rejection. Normal and -O commands both returned 0 with identical
output. This is NOT an old B3 numeric suite or a 5913 mode search. Historic C19
numeric outputs are retained as original bytes, not newly executed claims.

Actual lean/lake/elan probes found no executables. Both exact-profile replay
DRIVERS reported missing lake before compiling any file; their exit 0 is not
a compiler exit. Compiler runs and actual axioms are empty/null. Static source
escape scanning is not an imported axiom closure audit. Neither exact package
API compatibility nor root statement-faithfulness has been validated.

Repository target: Lean 4.33.0, Mathlib
db584cd6d46c92f209a44c0f1c829460d327499d.
Compatibility target: Lean 4.33.1, Mathlib
0df444a360eaa60ab8c11dca51a86af692955474.
These are requested configurations, not observed installed runtime fingerprints.
No literal external Prove2Me root source was obtained and no same-name weaker
root theorem was introduced. No new admission_request, scalar SMT fixture,
EvidenceLink, Result, Solution or trusted attestation was generated.

## Next theorem-sized obligations

1. Elaborate unchanged C16/C18 and frozen C19, then RecutAgreement at both exact
   locks; repair concrete API failures without changing the intended statements.
2. Replay B3Native.nongeodesic_split and OutsideSpan's finite minimum in the
   same representation, recording actual axioms and statement mapping.
3. Map the literal final root Cycle/EdgeWeight/IsGeodesic/IsPeripheral definitions
   to these native definitions; that external source remains unobserved.
4. Only after full B3 native replay, connect tight/nontight bridges and prove
   the separate outside-triangle-span seed using the root combinatorial tail.
5. Obtain distinct trusted replay, semantic review and the registered closure
   gate. Generator proofs, CI and merge never perform those operations.
